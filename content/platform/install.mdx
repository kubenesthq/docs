---
title: Install the platform
description: Install the KubeNest Platform bundle — a pinned, tested k3s stack — onto Ubuntu hosts you supply.
---

import { Callout, Steps } from 'nextra/components'

<Callout type="error">
**DRAFT — SPECIFICATION, NOT SHIPPED SOFTWARE.**

This page is the build specification for `kn-7k8` (installer), written before the installer
exists. Commands, flags, output and stage names on this page describe intended behaviour.
Do not follow this page against a real cluster, and do not quote it to a customer.

It is promoted to published only when `kn-ze1` has verified every claim on it against a real
cluster. Passing the test is what publishes the page.

Thirteen of the fourteen open questions were decided on 2026-08-13 and are recorded in
[Decisions](#decisions). One remains — host sizing, which needs measuring rather than deciding —
and is collected in [Still open](#still-open).
</Callout>

# Install the platform

The KubeNest Platform is a pinned, tested bundle of everything a Kubernetes cluster needs above
the control plane — ingress, certificates, storage, backup, upgrade orchestration and OS patching —
installed together as one versioned unit onto Ubuntu hosts you supply.

This page covers installing that bundle onto machines that have no Kubernetes on them yet.

<Callout type="info">
**This is not the operator install.** If you already run Kubernetes and only want to connect
it to a KubeNest control plane, see [Installation](/getting-started/installation) instead.
That page installs the KubeNest operator into an existing cluster. This page builds the cluster
and the platform layer underneath it.
</Callout>

## What "one click" means here

The claim is deliberately narrow, because a claim we can test is worth more than a claim that
sounds bigger:

> From a single machine with SSH access to your target nodes, one command installs the complete
> core bundle in under fifteen minutes, with no further human steps.

What that does **not** mean:

- It does not provision machines. You supply the hosts, the network and the disks.
- It does not configure your DNS, your load balancer or your firewall.
- It does not mean nothing can fail. It means a failure names the component, the stage and the
  remedy — see [When it fails](#when-it-fails).

## Before you start

### Supported hosts

**Ubuntu LTS only.** The installer refuses to run on a non-LTS Ubuntu release or on any other
distribution, rather than half-installing one.

Locking the operating system is what makes the bundle testable: a known kernel and a
deterministic LVM layout are preconditions for the storage and OS-patching components, and for
replicated storage later.

The specific LTS releases tested for a given bundle are recorded in that bundle's manifest, not
here, so that the supported set moves with the bundle rather than with this page. Pinning that
list, and setting the policy for what happens when a release reaches end of standard support
mid-bundle, belongs to `kn-boj`.

### Host sizing

<Callout type="warning">
**OQ-INSTALL-2 — minimum and recommended host specification. Not yet measured.**

This is the one open item on the page, and it is open because the number has to be measured
rather than decided. The core bundle is materially heavier than bare k3s: cert-manager, OpenEBS,
Velero, `system-upgrade-controller`, `kured` and the KubeNest agent all run continuously before a
single workload is scheduled. Publishing a guessed figure here is how a customer's first install
fails on an under-provisioned box.

Method, agreed: measure the idle resident CPU, memory and disk footprint of core on a
single-server install, and of core plus every profile, then state minimum and recommended figures
separately for server and agent nodes. Owned by `kn-ze1`, which will have the environment to
measure it. Until then the preflight resource thresholds have no values to check against.
</Callout>

### Disks and storage

The bundle uses OpenEBS Local PV LVM for persistent volumes. Replicated block storage is a
profile, not core — it is itself a source of operational dread, and durability in the default
configuration comes from verified backups plus application-level replication instead.

Local PV LVM provisions volumes out of an LVM volume group that must exist on each node that will
host persistent data. There are two supported ways to get one, and you pick per install.

**Option 1 — you create the volume group (default).** Create a volume group named `kubenest-vg`
on each data-bearing node before running the installer. Preflight verifies it exists and has free
extents. The installer never touches your block devices.

**Option 2 — the installer creates it.** Name a blank device explicitly:

```bash
--storage-device /dev/nvme1n1
```

The installer creates `kubenest-vg` on that device. Preflight refuses if the device holds a
partition table, a filesystem or an existing volume group, so this cannot silently overwrite data.
The flag is required — there is no device auto-detection, because guessing which disk is
disposable on someone else's infrastructure is not a risk worth taking.

Creating a volume group from free space on the root disk is not supported.

Which option you used is recorded against the cluster, because it determines what
[uninstall](#uninstall) is allowed to remove.

### Backup

Velero is installed as part of core. Configuring a backup target at install time is **optional**.

If you supply an S3-compatible target, Velero is configured and the first scheduled backup runs
per the schedule on the backup and restore page. If you do not, Velero is installed unconfigured
and the cluster reports `backup: unconfigured` in every fleet-telemetry heartbeat from the first
one onward. The console shows that as a warning against the cluster until a target is set.

An unconfigured backup is visible, not silent. A cluster that has never taken a backup is exactly
the quiet failure this product exists to prevent, so it is surfaced continuously rather than
blocking the install.

<Callout type="info">
Velero itself is not optional and is not a profile. Core is a fixed set — variation comes from
profiles only, or the tested-configuration count stops being N+2.
</Callout>

### Network

Between cluster nodes:

| Port | Protocol | Direction | Purpose |
|---|---|---|---|
| 6443 | TCP | agents → server | Kubernetes API |
| 8472 | UDP | all ↔ all | Flannel VXLAN overlay |
| 10250 | TCP | all ↔ all | Kubelet metrics |
| 2379–2380 | TCP | server ↔ server | Embedded etcd — **3-node HA tier only** |

Outbound from the cluster and from the machine running the installer:

- HTTPS to container registries and Helm repositories, for the duration of the install.
- HTTPS to your KubeNest control plane, from the installer.
- Persistent outbound WSS to your KubeNest hub, from the cluster. The agent dials out; no node
  needs a public address and no inbound port needs opening for KubeNest itself.

<Callout type="info">
**Air-gapped installs are not supported and are a roadmap item.** Both the install and the
cluster require outbound internet access. Supporting air-gapped operation means shipping the
bundle as a versioned image archive plus a local chart bundle, with its own release process. It
is planned, not present. Disconnected installs — installing with no control plane reachable —
are part of the same roadmap item, since they share the packaging work.
</Callout>

### Access

The machine you run the installer from needs SSH to every target node, with **passwordless sudo**
on each.

The installer uses your existing SSH setup: `~/.ssh/config`, `ssh-agent`, or a key file named
with `--ssh-key`. Key material stays on your machine. It is never uploaded to the control plane,
never stored by KubeNest, and never written to the installer's logs.

Preflight runs `sudo -n true` on each node and fails with the exact remediation if it does not
succeed. The default `ubuntu` user on official Ubuntu cloud images already has passwordless sudo
configured by cloud-init, so on a stock AWS Ubuntu host this needs nothing from you.

### An existing cluster on these hosts

**Not supported.** The installer builds new clusters only. Preflight refuses to run on any host
where k3s, RKE2, kubelet or containerd is already present, and says so plainly.

Adopting a cluster that already exists means inheriting whatever ingress, CSI and cert manager
are already installed on it — which is the untested component combination the bundle exists to
eliminate. It is a different product and it is out of scope for now.

## Choose your tiers before you run

Two choices are made at install time and recorded permanently against the cluster.

### HA tier

<Callout type="warning">
Migrating a single-server cluster to embedded etcd afterwards is disruptive and there is no
supported migration path. This is not a day-2 item. Choose deliberately.
</Callout>

**`single-server`** — one control-plane node, plus verified snapshots. Simpler, cheaper, fewer
moving parts. The honest promise is *"we restore within X hours"*, never *"highly available"*.

Be clear on what a single-server control-plane failure actually does: running pods keep running,
because the kubelet does not need the API server. But nothing new schedules, nothing self-heals,
no deploy or scale succeeds, and any pod or node that dies is never replaced. Ingress keeps
serving its last known configuration. The cluster is **frozen, not down** — until the second
failure, which cascades with no recovery path.

**`ha`** — three control-plane nodes with embedded etcd. Survives the loss of one node. Triples
control-plane cost and introduces etcd quorum operations.

<Callout type="info">
**OQ-INSTALL-9 — tier availability.** The `--ha` flag and the recorded tier value ship in the
first bundle regardless of when the 3-node tier itself lands, so that no cluster is ever ambiguous
about which tier it bought. If the tier is not yet available in the bundle you requested,
`--ha ha` fails with a clear "not available in bundle X" error rather than being undocumented or
silently downgrading.

**The tier itself must land before the first customer cluster carries production traffic**, because
there is no migration path. Every cluster installed before it exists is permanently single-server
unless rebuilt from scratch — a one-way door on each cluster deployed in the meantime.

Build cost is moderate: k3s supports embedded etcd directly via `--cluster-init` on the first
server, so the work is install orchestration, quorum verification, etcd snapshot handling in the
backup path, and upgrade sequencing in `kn-fuo` across three servers instead of one. Run cost is
two extra instances per cluster plus etcd quorum operations, which is dread we absorb on the
customer's behalf and must therefore price.

Exact sequencing against bundle 1 depends on Crest question 3 in the strategy document — *what
availability do your clients actually need*. Tracked in `kn-kp3`.
</Callout>

### Profiles

Core is always installed. Profiles are chosen at install and are the only supported way to vary
what a cluster runs — free-form component toggles would mean 2<sup>N</sup> test configurations
instead of N+2.

| Profile | Contents |
|---|---|
| `observability` | VictoriaMetrics, Grafana, Loki |
| `secrets` | sealed-secrets, or external-secrets against Vault or a cloud secret store |
| `replicated-storage` | Mayastor or Longhorn |
| `ha` | 3-node embedded etcd control plane |

Profiles, what changing one later does, and what it does not do, are covered on the Profiles page.

## The install command

The installer is the `kubenest` CLI, run from your own workstation or bastion. Your SSH keys and
your cloud credentials stay on your machine.

Authenticate to your control plane once:

```bash filename="terminal"
kubenest login --control-plane https://api.your-domain.com
```

Then install:

```bash filename="terminal"
kubenest platform install \
  --bundle 1.4 \
  --name crest-client-a \
  --server 10.0.1.10 \
  --agent  10.0.1.11 \
  --agent  10.0.1.12 \
  --ha single-server \
  --profile observability \
  --profile secrets \
  --ssh-user ubuntu \
  --ssh-key ~/.ssh/id_ed25519
```

`--bundle` selects the platform version. Every component version is pinned by that number — the
bundle is the unit of test, of support and of upgrade, and a cluster is always at exactly one
bundle version plus a recorded profile set. What each version contains is on the
Bundle contents page.

Re-running the same command against the same cluster is safe. The installer converges: it does
not duplicate resources and it does not fail because something is already present.

### A control plane is required

`kubenest platform install` registers the cluster with your control plane as part of the install,
and will not run without it. That is deliberate rather than incidental:

- The cluster's bundle version, profile set and HA tier have to be recorded somewhere durable, or
  no later upgrade is safe. You cannot upgrade a cluster whose contents you only think you know.
- Fleet health telemetry starts at install. Not knowing the state of a cluster is what turns
  support into an unbounded commitment.
- Upgrade orchestration, scoped per-client access and the restore drills all read that record.

Installing with no control plane reachable is part of the air-gapped roadmap item above.

### Provisioning the machines

`kubenest platform install` expects hosts to exist. It does not create them.

If you supply your own hosts — the Crest case, and the path the rest of this page documents —
that is all you need. If you want KubeNest to create the machines first, the CLI does that too,
as a separate command:

```bash filename="terminal"
kubenest cluster provision \
  --provider aws \
  --region ap-south-1 \
  --name crest-client-a \
  --instance-type t3.large \
  --agents 2 \
  --disk-size 100
```

It prints the resulting node addresses, which you then pass to `kubenest platform install`.

Provisioning and installing stay two commands rather than one. They fail in different ways and
are re-run under different circumstances, and a failed install must never re-run
`terraform apply`.

AWS is the only supported provider. GCP and Azure are out of scope.

<Callout type="info">
**Your cloud credentials stay on your machine**, exactly like your SSH keys. The CLI reads your
existing AWS credential chain — environment variables, `~/.aws/credentials`, or an instance
profile — and runs Terraform locally. Nothing is uploaded to the control plane.

That is why provisioning lives in the CLI rather than in the control plane (**OQ-INSTALL-14**).
Holding cloud credentials centrally while explicitly not holding SSH keys is hard to explain to a
security reviewer, and re-opens the procurement objection that choosing the CLI closed.

The Terraform modules already exist in `kubenest-backend/provisioner/terraform`. The port is
tracked in `kn-plcc`.
</Callout>

### Do customers manage clusters through the console?

For installing, no — the CLI is sufficient and self-contained. For everything after, yes. The
console and the control plane own the bundle and profile record, fleet health, upgrades, backup
and restore drills, and Crest's scoped per-client access. Day 1 is a CLI. Day 2 is the product.

## What the installer does, in order

Thirteen stages. Each emits a progress event so the console can show real progress rather than a
spinner, and each names itself in any failure.

The order is not arbitrary — every stage depends on the ones above it.

| # | Stage | What happens |
|---|---|---|
| 1 | `preflight` | Control-plane authentication, then every check in the table below, against every node |
| 2 | `register` | The cluster record is created and the agent JWT issued |
| 3 | `k3s-server` | k3s at the pinned version on the control-plane node, or all three for the `ha` tier |
| 4 | `k3s-agents` | Agents join the server |
| 5 | `platform-networking` | Traefik with the Gateway API provider, and the Gateway API CRDs |
| 6 | `platform-certs` | cert-manager |
| 7 | `platform-storage` | The volume group is verified or created, then OpenEBS Local PV LVM and the default StorageClass |
| 8 | `platform-backup` | Velero, configured if a target was supplied |
| 9 | `platform-day2` | `system-upgrade-controller` and `kured` |
| 10 | `kubenest-agent` | The KubeNest agent, the hub handshake, and the first telemetry heartbeat |
| 11 | `profiles` | Each selected profile, in the order listed above |
| 12 | `record` | The bundle version, profile set, HA tier and volume-group ownership are written against the cluster record |
| 13 | `verify` | The acceptance checks below are run and reported |

Stage 1 writes nothing anywhere. Stage 2 writes only to the control plane. The first change to any
of your machines happens in stage 3.

**The whole sequence must complete in under fifteen minutes**, for both reference shapes: a
single-server core install, and a three-node install with every profile enabled. `kn-ze1` asserts
this. If a measured install exceeds it, that is a defect to fix, not a number to revise upward.

<Callout type="info">
**Traefik with Gateway API, not ingress-nginx.** `kubernetes/ingress-nginx` reached end of life
on 24 March 2026. The repository is read-only: no features, no fixes and **no CVE patches** —
after four HIGH-severity CVEs disclosed together in February 2026 and the IngressNightmare
unauthenticated RCE before them. Traefik is already the k3s default, so this is the lighter
choice as well as the safe one.

Converting existing Ingress resources to Gateway API with `ingress2gateway` is **not** part of the
install flow. It is tracked separately as `kn-pgu`.
</Callout>

<Callout type="warning">
**Spec delta for `kn-7k8`.** The current `provisioner/entrypoint.sh` does the opposite of
stage 5: it installs k3s with `--disable traefik` and then installs ingress-nginx. It also does
not pin the k3s version, and installs none of stages 7, 8 or 9. Stage 12 depends on the manifest
schema from `kn-boj`, which does not exist yet. The installer is a rewrite of that path, not an
extension of it.
</Callout>

### Preflight checks

Preflight is the whole reason a failed install is cheap. Everything here is checked on every node
before the first byte is written anywhere, and any failure aborts before a machine is touched.

| Check | Fails when |
|---|---|
| Control plane | The control plane is unreachable, or the CLI is not logged in |
| SSH reachability | A node cannot be reached, or the key is rejected |
| Operating system | The node is not a supported Ubuntu LTS release |
| Privilege | `sudo -n true` fails |
| Existing Kubernetes | k3s, RKE2, kubelet or containerd is already present |
| Volume group | `kubenest-vg` is missing, or `--storage-device` names a device that is not blank |
| Node-to-node ports | Any port in the table above is blocked between nodes |
| Outbound egress | Registries, Helm repositories, the control plane or the hub are unreachable |
| Host resources | CPU, memory or disk below minimum (**OQ-INSTALL-2**) |
| Node count | Fewer than three control-plane nodes for `--ha ha` |
| Bundle availability | The requested bundle version does not exist, or does not offer the requested tier |

## Verifying the install

Stage 13 runs these automatically and reports the result. They are also the checks to run by hand
when something looks wrong, and they are the acceptance criteria `kn-ze1` will assert against a
real cluster.

<Steps>

### Every node is Ready

```bash filename="terminal"
kubectl get nodes
```

### Every core component is Running

```bash filename="terminal"
kubectl get pods -A
```

Traefik, cert-manager, OpenEBS, Velero, `system-upgrade-controller`, `kured` and the KubeNest
agent all reach Ready. No pod is in CrashLoopBackOff or Pending.

### Storage provisions a volume for real

A test PersistentVolumeClaim against the default StorageClass binds, rather than the
StorageClass merely existing. A StorageClass that cannot actually provision is the most common
way a storage install looks successful and is not.

### The cluster reports in

The cluster shows as connected in the console, and the first fleet-telemetry heartbeat has
arrived carrying the bundle version, the profile set, the HA tier and the backup configuration
state.

### The recorded manifest matches reality

The bundle version and profile set recorded against the cluster match what is actually installed
on it, component by component. Nothing else in the day-2 story is trustworthy if this drifts.

</Steps>

## When it fails

A failed install that says `error` is worse than no installer. Every failure reports three things:
**which stage**, **which component**, and **what to do next**.

**There is no automatic rollback.** A failed stage stops the install and leaves completed stages
in place. Automatic teardown would destroy the evidence needed to diagnose the failure, can itself
fail and leave a worse state, and is more expensive than resuming.

What makes that safe is that a half-installed cluster is never an unexplained state:

- **The installer keeps a journal.** Each completed stage is recorded, locally and against the
  cluster record. Resume is deterministic — it reads the journal rather than relying on every
  component happening to be idempotent.
- **The cluster is marked `install-failed`** in the control plane, carrying the failing stage.
  It never appears as a healthy managed cluster. Fleet telemetry knows about it immediately, so
  a support call about it is never a surprise.
- **There are exactly two supported exits**, and the failure message prints both.

To recover, either:

```bash filename="terminal"
# Resume: fix what the error names, then re-run the identical command.
kubenest platform install ...   # skips completed stages, resumes at the failure
```

```bash filename="terminal"
# Or start over: return the hosts to a known state.
kubenest platform uninstall --confirm
```

**If preflight failed**, nothing was written to any node and no cluster record exists. Fix the
reported condition and re-run. This is the common case, and it is why preflight is thorough.

## Uninstall

Enterprise buyers ask about the exit before they commit to the entry, so the exit is documented
and tested rather than improvised.

```bash filename="terminal"
kubenest platform uninstall --confirm
```

This removes k3s and every component the installer placed, and leaves the machines in a known
state.

**Uninstall never destroys data by default.** Persistent volumes and their contents survive.

To remove them, pass `--destroy-data` as well. Even then, uninstall only removes the
`kubenest-vg` volume group if the installer created it — recorded at install time. A volume group
you created yourself is never removed, on either path.

## Decisions

Recorded 2026-08-13. These were open questions on the first draft of this page; they are settled
and the page above reflects them.

| ID | Question | Decision |
|---|---|---|
| OQ-INSTALL-1 | Which Ubuntu releases? | LTS only for now. The tested release list per bundle belongs to `kn-boj`'s manifest |
| OQ-INSTALL-3 | Who creates the LVM volume group? | Both options. Customer-created `kubenest-vg` is the default; installer-created requires an explicit `--storage-device`. Root-disk free space is not supported |
| OQ-INSTALL-4 | Backup target required at install? | Optional. Velero installs unconfigured and the cluster reports `backup: unconfigured` until a target is set |
| OQ-INSTALL-5 | Air-gapped installs | Roadmap item. Not supported in bundle 1.x, stated in writing. Disconnected install folds into the same item |
| OQ-INSTALL-6 | Privilege and credential model | CLI-side keys via existing SSH setup, never uploaded or stored. Passwordless sudo required, checked in preflight |
| OQ-INSTALL-7 | Entry point | The `kubenest` CLI |
| OQ-INSTALL-8 | Adopt existing clusters? | No. New clusters only. Preflight refuses hosts that already run Kubernetes |
| OQ-INSTALL-9 | Does the 3-node etcd tier ship in bundle 1? | The `--ha` flag and recorded tier ship in bundle 1 regardless; `--ha ha` errors clearly if the tier is unavailable. The tier must land before any customer cluster carries production traffic, since there is no migration path. Exact sequencing needs Crest question 3 — `kn-kp3` |
| OQ-INSTALL-10 | Install duration budget | Fifteen minutes, both reference shapes. An overrun is a defect, not a revised number |
| OQ-INSTALL-11 | What may uninstall destroy? | Nothing by default. `--destroy-data` removes volumes. Never removes a volume group it did not create |
| OQ-INSTALL-12 | `ingress2gateway` in the install flow? | No. Tracked separately as `kn-pgu` |
| OQ-INSTALL-13 | Roll back a failed install? | No automatic rollback. Stage journal, `install-failed` cluster state, and exactly two exits: resume or uninstall |
| OQ-INSTALL-14 | Where does machine provisioning live? | In the CLI, as `kubenest cluster provision`. Cloud credentials stay customer-side, like SSH keys. Port tracked in `kn-plcc` |

## Still open

| ID | Question | Blocks |
|---|---|---|
| OQ-INSTALL-2 | Minimum and recommended host sizing — to be measured, not decided | Preflight resource thresholds, sizing guidance for customers |

Nothing else on this page is undecided. `OQ-INSTALL-2` is waiting on an environment to measure in,
which arrives with `kn-0i0`, and is then owned by `kn-ze1`.
