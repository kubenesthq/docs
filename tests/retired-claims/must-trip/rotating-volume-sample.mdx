# Fixture: this page MUST trip the guard

Deliberately wrong prose. Not published — see tests/retired-claims/README.md.

Every week the drill restores a rotating sample of your own volumes, so over a month it has restored
real data from most of the cluster instead of proving the same proof set again.
