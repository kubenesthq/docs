# Fixture: this page MUST trip the guard

Deliberately wrong prose. Not published — see tests/retired-claims/README.md.

Kernel livepatching is respected on every host: a node whose kernel was patched live is not rebooted
for that patch, because the coordinator reads the node's own patch state.
