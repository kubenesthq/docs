# Fixture: this page MUST NOT trip the guard

Prose that carries a listed command the right way. If the guard fires here it is over-matching,
which blocks a publish and trains people to bypass the gate.

<Callout type="warning">
**Not supported by the current release.** `kubenest health` is not in the command tree yet; it
arrives with Bundle 1.2, on promotion.
</Callout>

<Callout type="warning">
**Not supported by the current release.** `kubenest node reboot` is not in the command tree; it
arrives with Bundle 1.2, on promotion.
</Callout>

<Callout type="warning">
**Not supported by the current release.** `kubenest backup restore` is registered but not built; it
arrives with Bundle 1.2, on promotion.
</Callout>

A permanent warning that is not this one sits beside them without weakening them:

<Callout type="info">
**A callout that is not the marker.** A sealing key whose loss is unrecoverable deserves a warning
of its own, and it must not be mistaken for the unsupported-command one.
</Callout>
