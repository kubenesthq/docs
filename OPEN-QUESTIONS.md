# Day-2 platform — open design questions

Consolidated index of every `OQ-*` in `content/platform/`. Generated by hand on 2026-08-13 from
the eight `kn-nlaf` pages; **update it when you change a page.**

This file lives at the repo root, **outside `content/`**, so Nextra cannot route it and it can
never be published to docs.kubenest.io. Keep it that way — it names unmade decisions and
unmeasured numbers.

**43 questions. 15 decided, 28 open.** Last reconciled 2026-08-20 against decision A.

Each question is written up in full on its page, inline where it arises, with the reasoning and a
recommendation where there is one. This file is the index, not the content.

---

## Blocking the first bundle release

Nothing ships until these are answered. Six of them.

| ID | Question | Page |
|---|---|---|
| OQ-BUNDLE-1 | What do the version digits mean, and what forces a bump? Does a CVE patch bump the bundle? | bundle |
| OQ-BUNDLE-2 | How far behind upstream k3s do we track? | bundle |
| OQ-BUNDLE-3 | Response to a component CVE between releases | bundle |
| OQ-BUNDLE-6 | Actual pinned versions for release 1 — a task, not a design question, but it blocks | bundle |
| OQ-BUNDLE-8 | Values for `limits.timeouts` and `limits.resources`. The structure is settled; the numbers are provisional | bundle |
| OQ-PROFILE-2 | `secrets` offers a choice inside a slot, which the matrix rule forbids | profiles |
| OQ-PROFILE-4 | Which replicated storage engine — Mayastor, Longhorn, or Piraeus/LINSTOR? | profiles |

**OQ-BUNDLE-3 is the one with teeth.** We sell the ingress-nginx EOL as a reason to buy. A
prospect who has heard that argument will ask what our own CVE response looks like, and we do not
currently have an answer.

## Decide before the first customer install

Not release blockers, but each is a one-way door for any cluster installed before it is settled.

| ID | Question | Page |
|---|---|---|
| ~~OQ-HA-1~~ | **DECIDED 2026-08-20 — embedded etcd on every tier.** See below | ha |
| OQ-BUNDLE-5 | Agent ↔ control plane compatibility window | bundle |

**OQ-HA-1 was the largest open question in the set, and it is now answered: single-node embedded
etcd (`k3s --cluster-init`) on every tier, including `single-server`.** Decision A of the build
plan, pinned in `kubenest-contracts/bundles/platform-1.0.yaml` — `k3s: v1.35.7+k3s1`.

Three consequences, and it is worth being precise about which are real today:

- **OQ-BACKUP-4 is resolved.** One snapshot mechanism, not two. Every tier takes an etcd snapshot.
- **The one-way door between tiers is no longer a datastore problem.** `single-server → ha` is
  joining two servers to an existing etcd cluster. But the join is **not implemented and not
  tested** (`kn-kp3`), so the tier is still an install-time choice in practice. The pages say
  exactly this, and must not be "corrected" into promising a migration.
- **OQ-INSTALL-9's reasoning is weakened** — it leaned on the one-way door — though the decision
  it reached still stands.

**OQ-BUNDLE-5** is a compatibility axis that is not in the six tested configurations at all. The
bundle pins the agent; the control plane upgrades separately. Without a stated support window, the
first control-plane upgrade breaks every cluster on an older bundle.

## Needs measurement, not decision

All owned by `kn-ze1`, all blocked on `kn-0i0`'s ephemeral environment. **Two of them block
publishing pages.**

| ID | What to measure | Page |
|---|---|---|
| OQ-INSTALL-2 | Minimum and recommended host sizing. Provisional figures are in place; preflight warns rather than fails against them | install |
| OQ-BACKUP-2 | Full cluster restore duration — **X in "we restore within X hours"** | backup-restore |
| OQ-HA-2 | Same number, seen from the tier that promises it. Blocked on OQ-BACKUP-2 | ha |
| OQ-UPGRADE-6 | Upgrade duration as fixed overhead plus per-node cost | upgrades |

**OQ-BACKUP-2 / OQ-HA-2 is the most important unmeasured number in the product.** The
`single-server` tier's entire honest promise is a sentence with a blank in it. Do not quote that
tier to a customer with a number attached until it is measured.

## Needs Crest

Five assumptions are already recorded in `STRATEGY-2026-08.md` §9.3 with their basis and what
unwinding each would cost. These are the ones still genuinely waiting on an answer:

| ID | Question | Crest question | Page |
|---|---|---|---|
| OQ-INSTALL-9 | Does the 3-node etcd tier ship in bundle 1? Assumed yes | §9 Q2 — availability | install |
| OQ-PROFILE-4 | Replicated storage engine — informed by whether clients need it at all | §9 Q2 | profiles |

## Everything else, by page

### `install.mdx` — 14 questions, 13 decided

Only **OQ-INSTALL-2** remains open (see measurement, above). The other thirteen are recorded in
the page's `## Decisions` table. Four of those were closed on an assumption rather than an answer
— OQ-INSTALL-5, -8, -9 and -12 — and are mirrored in STRATEGY §9.3.

### `bundle.mdx` — 7 open

Release blockers above, plus:

| ID | Question |
|---|---|
| OQ-BUNDLE-4 | Concurrent supported bundles; can upgrades skip versions? Blocks OQ-UPGRADE-5 |
| OQ-BUNDLE-7 | Is the manifest published publicly? Recommend yes |
| OQ-BUNDLE-8 | Provisional values for every threshold and timeout in `limits` |

### `profiles.mdx` — 5 open

Release blockers above, plus:

| ID | Question |
|---|---|
| OQ-PROFILE-1 | Two profiles interact with core's Velero; `kn-twe` assumes orthogonality |
| OQ-PROFILE-3 | Verify sealed-secrets' Bitnami lineage before locking |
| OQ-PROFILE-5 | Does adding a profile reuse the upgrade machinery? |

### `upgrades.mdx` — 7 open

| ID | Question |
|---|---|
| OQ-UPGRADE-1 | Does the deprecation scan cover Git as well as live workloads, and does a Git finding block? |
| OQ-UPGRADE-2 | Deprecation dataset source, and whether the block can be overridden |
| OQ-UPGRADE-3 | Refusing to start when the maintenance window is too small. Needs OQ-UPGRADE-6 |
| OQ-UPGRADE-4 | Is rollback automatic on failure, or always operator-initiated? |
| OQ-UPGRADE-5 | Can upgrades skip bundle versions? Depends on OQ-BUNDLE-4 |
| OQ-UPGRADE-6 | Upgrade duration (measurement, above) |
| OQ-UPGRADE-7 | Operator-initiated only, or opt-in automatic? |

### `backup-restore.mdx` — 3 open

| ID | Question |
|---|---|
| OQ-BACKUP-1 | Does the drill restore every volume or a sample? Whatever is chosen, the result must state what it verified |
| OQ-BACKUP-2 | Full cluster restore duration (measurement, above) |
| OQ-BACKUP-3 | Default schedules and retention |
| ~~OQ-BACKUP-4~~ | **RESOLVED by OQ-HA-1 (decision A).** Every tier runs etcd, so there is one snapshot path, not two |

### `os-patching.mdx` — 3 open

| ID | Question |
|---|---|
| OQ-PATCH-1 | Pending-reboot alert threshold, and handling of critical CVEs |
| OQ-PATCH-2 | Is kernel livepatching supported, respected, or out of scope? |
| OQ-PATCH-3 | The exact `unattended-upgrades` policy |

### `ha.mdx` — 2 open

Both above. `deploying-an-app.mdx` has none.

---

## Cross-page dependencies

Answer these in order; several unlock others.

```
OQ-HA-1  DECIDED 2026-08-20: embedded etcd on every tier
   ├─→ OQ-BACKUP-4   RESOLVED — one snapshot mechanism
   └─→ weakened the reasoning behind OQ-INSTALL-9; that decision still stands

OQ-BUNDLE-4  (can upgrades skip versions?)
   └─→ OQ-UPGRADE-5

OQ-BUNDLE-2  (k3s tracking policy)
   └─→ OQ-BUNDLE-6   actual pinned versions

OQ-UPGRADE-6  (upgrade duration)
   └─→ OQ-UPGRADE-3  refusing an upgrade that cannot fit its window

OQ-BACKUP-2  (restore duration)
   └─→ OQ-HA-2       the single-server promise
```

## Where else these live

- **In full, with reasoning:** inline in each page, and in that page's summary table
- **On the beads that build them:** comments on `kn-7k8`, `kn-boj`, `kn-twe`, `kn-ynaq`, `kn-fuo`,
  `kn-mzn`, `kn-nqj`, `kn-kp3`, `kn-ze1`
- **Crest-dependent assumptions:** `STRATEGY-2026-08.md` §9.3, with what to unwind if wrong
